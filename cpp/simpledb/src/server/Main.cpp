#include <stdio.h>
#include <iostream>
#include <sstream>
#include <string>

int main(int argc, char **argv)
{
    // Supports operations like
    // 1) load existing index
    // 2) create new index
    // 3) CRUD ops on an index
    // 4) Delete an index
}